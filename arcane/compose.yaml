services:
  arcane:
    # nosemgrep
    image: ghcr.io/getarcaneapp/manager:v2.8.0@sha256:9146935e0797ce14a340616ceab2f77b3bc61d1b10befb0718a4be7f01485e2c
    environment:
      APP_URL: ${APP_URL:-http://localhost:3552}
      PUID: ${PUID:-}
      PGID: ${PGID:-}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:?}
      JWT_SECRET: ${JWT_SECRET:?}
      LOG_JSON: true
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
      - SETGID
      - SETUID
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    read_only: true
    volumes:
      - /apps/arcane/data:/app/data
      - /apps/arcane/cache:/builds
      - /var/run/docker.sock:/var/run/docker.sock
    tmpfs:
      - /tmp
    ports:
      - 3552:3552
    healthcheck:
      test: [CMD, /app/arcane, health]
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: 1
          memory: 4g
          pids: 512
    labels:
      caddy: "*.{$$DOMAIN}"
      caddy.import: reverse_proxy_auth arcane arcane:3552

networks:
  default:
    name: proxy
    external: true
