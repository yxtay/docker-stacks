services:
  arcane:
    # nosemgrep
    image: ghcr.io/getarcaneapp/manager:v2.12.0@sha256:7304d54948626355e11a855dbcde49f58468c71e4cbf43b5315d3a2f5fd01a58
    environment:
      APP_URL: ${APP_URL:-http://localhost:3552}
      PUID: ${PUID:-}
      PGID: ${PGID:-}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:?}
      JWT_SECRET: ${JWT_SECRET:?}
      LOG_JSON: true
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
      - SETGID
      - SETUID
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    read_only: true
    volumes:
      - /apps/arcane/data:/app/data
      - /apps/arcane/cache:/builds
      - /var/run/docker.sock:/var/run/docker.sock
    tmpfs:
      - /tmp
    ports:
      - 3552:3552
    healthcheck:
      test: [CMD, /app/arcane, health]
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: 1
          memory: 4g
          pids: 512
    labels:
      caddy: "*.{$$DOMAIN}"
      caddy.import: reverse_proxy_auth arcane arcane:3552

networks:
  default:
    name: proxy
    external: true
