services:
  resticprofile:
    image: creativeprojects/resticprofile:latest
    command: [schedule, --all]
    environment:
      RCLONE_CONFIG_GDRIVE_TYPE: drive
      RCLONE_CONFIG_GDRIVE_SCOPE: drive
      RCLONE_CONFIG_GDRIVE_SERVICE_ACCOUNT_FILE: /run/secrets/google_service_account
    secrets:
      - google_service_account
      - restic_password
    volumes:
      - ./resticprofile:/etc/resticprofile:ro
      - /apps:/apps:ro
      - /apps/resticprofile/cache:/var/cache/restic
    restart: unless-stopped

secrets:
  google_service_account:
    environment: GOOGLE_SERVICE_ACCOUNT_JSON
  restic_password:
    environment: RESTIC_PASSWORD
