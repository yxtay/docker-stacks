services:
  immich_server:
    # nosemgrep
    image: ghcr.io/immich-app/immich-server:v2.7.5@sha256:c15bff75068effb03f4355997d03dc7e0fc58720c2b54ad6f7f10d1bc57efaa5
    # extends:
    #   file: hwaccel.transcoding.yml
    #   service: cpu # set to one of [nvenc, quicksync, rkmpp, vaapi, vaapi-wsl] for accelerated transcoding
    healthcheck:
      test: [CMD, bash, -c, ":> /dev/tcp/localhost/2283"]
    environment:
      TZ: ${TZ:-UTC}
    env_file:
      - .env
    volumes:
      - ${UPLOAD_LOCATION:?}:/data
      - ${LIBRARY_LOCATION:?}:/data/library
    expose:
      - 2283
    networks:
      - default
      - caddy
    depends_on:
      redis:
        condition: service_healthy
      database:
        condition: service_healthy
    restart: unless-stopped
    labels:
      caddy: "*.{$$DOMAIN}"
      caddy.import: reverse_proxy immich immich_server:2283

  immich_machine_learning:
    # nosemgrep
    # For hardware acceleration, add one of -[armnn, cuda, rocm, openvino, rknn] to the image tag.
    image: ghcr.io/immich-app/immich-machine-learning:v2.7.5@sha256:a2501141440f10516d329fdfba2c68082e19eb9ba6016c061ac80d23beadf7f3
    # extends:
    #   file: hwaccel.ml.yml
    #   service: cpu # set to one of [armnn, cuda, rocm, openvino, openvino-wsl, rknn] for accelerated inference
    healthcheck:
      test: [CMD, bash, -c, ":> /dev/tcp/localhost/3003"]
    env_file:
      - .env
    volumes:
      - /apps/immich/model/cache:/cache
    networks:
      default:
        aliases:
          - immich-machine-learning
    restart: unless-stopped

  redis:
    # nosemgrep
    image: ghcr.io/valkey-io/valkey:9.1.0@sha256:4963247afc4cd33c7d3b2d2816b9f7f8eeebab148d29056c2ca4d7cbc966f2d9
    healthcheck:
      test: [CMD, valkey-cli, ping]
    restart: unless-stopped

  database:
    # nosemgrep
    image: ghcr.io/immich-app/postgres:18-vectorchord0.5.3@sha256:71964cc1e52ae1db784bc3ed6eeb366242e00a37e334456bd05b47543d76b76f
    healthcheck:
      test: [CMD, pg_isready]
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD:?}
      POSTGRES_USER: ${DB_USERNAME:-postgres}
      PGUSER: ${DB_USERNAME:-postgres}
      POSTGRES_DB: ${DB_DATABASE_NAME:-immich}
      PGDATABASE: ${DB_DATABASE_NAME:-immich}
      POSTGRES_INITDB_ARGS: --data-checksums
    volumes:
      - ${DB_DATA_LOCATION:?}:/var/lib/postgresql
    shm_size: 128mb
    restart: unless-stopped

  gphotos2immich:
    # nosemgrep
    image: ghcr.io/warreth/gphotos2immich:v1.6.1@sha256:cf748ba1c1ddfe291bb03ba101c0c44a426ee1a6d66133420a2db18130714529
    healthcheck:
      test: [CMD, wget, -qO-, "http://localhost:8080/"]
    environment:
      PORT: "8080"
      DISABLE_WEBUI: "false"
    volumes:
      - /apps/gphotos2immich/config.json:/app/config.json
      - /apps/gphotos2immich/data:/app/data
    expose:
      - 8080
    networks:
      - caddy
    restart: unless-stopped
    labels:
      caddy: "*.{$$DOMAIN}"
      caddy.import: reverse_proxy gphotos2immich gphotos2immich:8080

  immich_kiosk:
    # nosemgrep
    image: ghcr.io/damongolding/immich-kiosk:0.39.3@sha256:b65371b9fbe93cde8355c06ba095fb4801bbb5d7c8c51065a5bfa02183536771
    tty: true
    healthcheck:
      test: [CMD, curl, -fsSL, "http://localhost:3000/health"]
    environment:
      LANG: en_US
      TZ: ${TZ:-UTC}
    volumes:
      - /apps/immich_kiosk:/config
    expose:
      - 3000
    networks:
      - caddy
    restart: unless-stopped
    labels:
      caddy: "*.{$$DOMAIN}"
      caddy.import: reverse_proxy immich_kiosk immich_kiosk:3000

networks:
  default:
  caddy:
    name: caddy
    external: true
