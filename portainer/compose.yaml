services:
  portainer:
    image: portainer/portainer-ce:lts
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /data/portainer:/data
    ports:
      - 9443:9443
    expose:
      - 8000
    restart: unless-stopped
