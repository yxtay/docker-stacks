services:
  portainer:
    # nosemgrep
    image: portainer/portainer-ce:2.42.0@sha256:c3c919ec68d130cbcb837693420592b91afd320b28b492c16c84ec5561bbf095
    command: --data ${PORTAINER_DATA:?} --log-mode JSON
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    read_only: true
    volumes:
      - ${PORTAINER_DATA}:/data
      - ${PORTAINER_DATA}:${PORTAINER_DATA}
      - /var/run/docker.sock:/var/run/docker.sock:ro
    ports:
      - 9443:9443
    expose:
      - 8000
      - 9000
    # network_mode: host  # required for initial setup
    healthcheck:
      test: [CMD, /portainer, --version]
    restart: unless-stopped
    labels:
      caddy: "*.{$$DOMAIN}"
      caddy.import: reverse_proxy_auth portainer portainer:9000

networks:
  default:
    name: caddy
    external: true
