services:
  sencho:
    image: ghcr.io/studio-saelix/sencho:0.93.2@sha256:53d1469de4924bfc326d77db10cc975c6549a0a7a1afa219c6c718633333d910
    environment:
      COMPOSE_DIR: ${COMPOSE_DIR:?}
    cap_drop:
      - ALL
    cap_add:
      - DAC_OVERRIDE
      - FOWNER
    security_opt:
      - no-new-privileges:true
    read_only: true
    volumes:
      - /apps/sencho:/app/data
      - ${COMPOSE_DIR:-${HOME}}:${COMPOSE_DIR:-${HOME}}
      - /var/run/docker.sock:/var/run/docker.sock:ro
    tmpfs:
      - /tmp
    ports:
      - 1852:1852
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: 1
          memory: 4g
          pids: 512
    labels:
      caddy: "*.{$$DOMAIN}"
      caddy.import: reverse_proxy_auth sencho {{ upstreams 1852 }}
      tinyauth.apps.sencho.path.allow: \/pilot

networks:
  default:
    name: proxy
    external: true
